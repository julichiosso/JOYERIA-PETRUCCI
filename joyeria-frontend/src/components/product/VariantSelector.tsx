/**
 * components/product/VariantSelector.tsx
 *
 * NOTA: ProductVariant no existe en el backend actual.
 * El modelo solo tiene `variantLabel: string | null` en Product,
 * que se muestra como texto informativo en la ficha de producto.
 *
 * Este componente queda como stub vacío. Si en el futuro el backend
 * implementa un modelo de variantes real, se construye aquí.
 */

export default function VariantSelector() {
  return null;
}
