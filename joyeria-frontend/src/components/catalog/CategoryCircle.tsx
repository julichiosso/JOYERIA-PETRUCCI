/**
 * components/catalog/CategoryCircle.tsx
 * Círculo de categoría estilo "highlights de Instagram" para la sección
 * "Explorar Colecciones" del mockup aprobado.
 * Se construye en la etapa de componentes de catálogo.
 */

import type { Category } from "@/types/category";

interface CategoryCircleProps {
  category: Category;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function CategoryCircle({ category }: CategoryCircleProps) {
  return null;
}
